{
  "name": "pallas-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Pallas web app on Cloudflare Workers (D1 + KV), separate from the Electron/Ollama desktop build",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pallas --remote --file=./schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cheerio": "^1.0.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
